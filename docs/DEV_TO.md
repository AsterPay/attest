# Dev.to article draft — EU AI Act + TypeScript

**Title options:**

1. EU AI Act and your Next.js app: a TypeScript-first compliance checklist (2026)
2. I added `attest scan` to our repo — here is what we learned

**Outline:**

1. **Hook:** Deadlines and fines get attention; engineers need actionable steps.
2. **What the Act expects (high level):** risk categories, transparency for many chatbots, documentation and logging for high-risk — always defer to counsel.
3. **What Attest does:** static scan for AI SDKs, heuristic risk hint, `attest check` signals, `attest docs` scaffolds, audit JSONL.
4. **Walkthrough:** install CLI, run on a sample repo, paste anonymised output.
5. **Limitations:** no substitute for legal review; heuristics can false-positive/negative.
6. **CTA:** try `github.com/AsterPay/attest`, star if useful.

**Tags:** `eu`, `ai`, `typescript`, `compliance`, `opensource`
