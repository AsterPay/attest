# Attest — naming & domains

**Product name:** Attest  
**npm scope:** `@asterpay/attest` (CLI + programmatic re-exports), `@asterpay/attest-sdk` (core library)

## Domain checklist (verify before purchase)

| Domain        | Purpose        | Status   |
|---------------|----------------|----------|
| `attest.dev`  | Primary        | Check registrar |
| `getattest.dev` | Marketing CTA | Check registrar |
| `attest.eu`   | EU positioning | Check registrar |

**Alternative brand:** Clearance (`clearance.dev`) — only if `attest.*` is unavailable.

Do not mention Finland/Helsinki in public copy; use EU/Europe only.
